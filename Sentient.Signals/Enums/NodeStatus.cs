namespace Sentient.Signals.Enums
{
    /// <summary>
    /// This Enum specifies the current status of the Node. Useful for logging.
    /// </summary>
    public enum NodeStatus
    {
        None = 0,
        Start = 1,
        Stop = 2,
        Exception = 3
    }
}
