namespace Sentient.Signals.Enums
{
    /// <summary>
    /// This enum specifies the outcome of the signal being sent.
    /// </summary>
    public enum Outcome
    {
        Failure = 0,
        Success = 1,
    }
}
